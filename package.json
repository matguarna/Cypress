{
  "devDependencies": {
    "cypress": "^11.2.0",
    "cypress-xpath": "^2.0.1"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run -b chrome"
  }
}
